name: Issue 367 Production MariaDB Maintenance Harness

on:
  workflow_dispatch:

jobs:
  gated:
    runs-on: ubuntu-latest
    steps:
      - name: Gate
        run: |
          echo 'Issue #367 maintenance harness is intentionally manual/gated.'
          echo 'Snapshot exists. Current deploy sudo secret was rejected and root SSH with the deploy key is unavailable.'
