name: Issue 367 Production MariaDB Maintenance Harness

on:
  workflow_dispatch:

jobs:
  gated:
    runs-on: ubuntu-latest
    steps:
      - name: Gate
        run: |
          echo 'Issue #367 maintenance harness is intentionally manual/gated.'
          echo 'Snapshot exists. Sudo credential still requires correction/verification before production mutation.'
