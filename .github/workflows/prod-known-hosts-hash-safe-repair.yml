name: Governed PROD known_hosts hash-safe local repair

on:
  issue_comment:
    types:
      - created

permissions:
  contents: read

concurrency:
  group: agency-prod-known-hosts-hash-safe-repair
  cancel-in-progress: false

jobs:
  validate-authority:
    name: Validate fresh owner #842 hash-safe repair authority
    if: >-
      ${{
        github.event.issue.pull_request == null &&
        github.event.issue.number == 842 &&
        startsWith(github.event.comment.body, '/agency-prod-known-hosts-hash-safe-repair repair ')
      }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      issues: read
    outputs:
      request_id: ${{ steps.authority.outputs.request_id }}
      authority_comment_id: ${{ steps.authority.outputs.authority_comment_id }}
      main_sha: ${{ steps.authority.outputs.main_sha }}
    steps:
      - name: Validate actor, one-shot request and exact live main
        id: authority
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          TRIGGER_COMMENT: ${{ github.event.comment.body }}
          COMMENT_AUTHOR: ${{ github.event.comment.user.login }}
          AUTHORITY_COMMENT_ID: ${{ github.event.comment.id }}
          EVENT_DEFAULT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          [[ "$GITHUB_ACTOR" == 'E-merging-digital' ]]
          [[ "$COMMENT_AUTHOR" == "$GITHUB_ACTOR" ]]
          [[ "$ISSUE_NUMBER" == '842' ]]
          [[ "$GITHUB_RUN_ATTEMPT" == '1' ]] || {
            echo '#842 repair authority cannot be replayed through a workflow rerun.' >&2
            exit 1
          }
          [[ "$AUTHORITY_COMMENT_ID" =~ ^[0-9]+$ ]]

          read -r command verb request_id requested_main extra <<< "$TRIGGER_COMMENT"
          [[ -z "${extra:-}" ]]
          [[ "$command" == '/agency-prod-known-hosts-hash-safe-repair' ]]
          [[ "$verb" == 'repair' ]]
          [[ "$request_id" =~ ^[A-Za-z0-9._-]{8,80}$ ]]
          [[ "$requested_main" =~ ^[0-9a-f]{40}$ ]]
          expected="/agency-prod-known-hosts-hash-safe-repair repair $request_id $requested_main"
          [[ "$TRIGGER_COMMENT" == "$expected" ]]

          issue_json="$(gh api "repos/$GITHUB_REPOSITORY/issues/842")"
          [[ "$(jq -r '.state' <<< "$issue_json")" == 'open' ]]
          [[ "$(jq -r '.pull_request // empty' <<< "$issue_json")" == '' ]]
          [[ "$(jq -r '.user.login' <<< "$issue_json")" == 'E-merging-digital' ]]

          live_main="$(gh api "repos/$GITHUB_REPOSITORY/branches/main" --jq '.commit.sha')"
          [[ "$requested_main" == "$live_main" ]] || {
            echo 'Requested main SHA is stale.' >&2
            exit 1
          }
          [[ "$EVENT_DEFAULT_SHA" == "$live_main" ]] || {
            echo '#842 repair workflow must execute tooling from exact live main.' >&2
            exit 1
          }

          comments="$(gh api --paginate "repos/$GITHUB_REPOSITORY/issues/842/comments?per_page=100" --jq '.[].body')"
          request_prefix="/agency-prod-known-hosts-hash-safe-repair repair $request_id "
          match_count=0
          while IFS= read -r body; do
            if [[ "$body" == "$request_prefix"* ]]; then
              match_count=$((match_count + 1))
            fi
          done <<< "$comments"
          [[ "$match_count" -eq 1 ]] || {
            echo 'request_id must be fresh and appear in exactly one owner authority comment.' >&2
            exit 1
          }

          echo "request_id=$request_id" >> "$GITHUB_OUTPUT"
          echo "authority_comment_id=$AUTHORITY_COMMENT_ID" >> "$GITHUB_OUTPUT"
          echo "main_sha=$live_main" >> "$GITHUB_OUTPUT"

      - name: Checkout exact trusted main tooling
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.authority.outputs.main_sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Validate repository-pinned #830 authority and offline-only tooling
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = '${{ steps.authority.outputs.main_sha }}'
          bash -n scripts/production-ssh-trust/hash-safe-repair-known-hosts.sh
          bash -n scripts/production-ssh-trust/repair-known-hosts.sh
          bash -n scripts/production-ssh-trust/manage-known-host.sh
          read -r key_type key_blob _ < scripts/production-ssh-trust/prod-ed25519.pub
          test "$key_type" = 'ssh-ed25519'
          test "$key_blob" = 'AAAAC3NzaC1lZDI1NTE5AAAAIHBzADlwNcSFBP6sYfGBH2SvD5NymTF6n2Ze996V4TqR'
          fingerprint="$(ssh-keygen -lf scripts/production-ssh-trust/prod-ed25519.pub -E sha256 | awk '{print $2}')"
          test "$fingerprint" = 'SHA256:Pflpbgh2vc9dUYe4fpXPxdwzhPqyy8vmbAOcS+BRLDQ'
          grep -Fxq "$fingerprint" scripts/production-ssh-trust/prod-ed25519.sha256

  repair-runner-local-trust:
    name: Repair only local PROD known_hosts via #842 offline work copy
    needs: validate-authority
    runs-on:
      - self-hosted
      - linux
      - x64
      - agency
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout exact trusted main tooling
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.validate-authority.outputs.main_sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Require exact Agency runner and prohibit synthetic hooks
        shell: bash
        run: |
          set -euo pipefail
          [[ "$RUNNER_NAME" == 'agency-browser-runner-01' ]] || {
            echo '#842 repair is restricted to agency-browser-runner-01.' >&2
            exit 1
          }
          unset AGENCY_842_SYNTHETIC_TEST AGENCY_842_TEST_FORCE_POST_VERIFY_FAILURE

      - name: Persist metadata-only pre-state before any mutation attempt
        id: prestate
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SERVER_HOST"
          unset AGENCY_842_SYNTHETIC_TEST AGENCY_842_TEST_FORCE_POST_VERIFY_FAILURE
          classification="$(SERVER_HOST="$SERVER_HOST" bash scripts/production-ssh-trust/hash-safe-repair-known-hosts.sh CLASSIFY)"
          value() {
            local key="$1" data="$2"
            awk -F= -v key="$key" '$1 == key { print substr($0, index($0, "=") + 1); exit }' <<< "$data"
          }
          marked_match="$(value MARKED_MATCH_PRESENT_BEFORE "$classification")"
          [[ "$marked_match" == 'YES' || "$marked_match" == 'NO' ]]
          mkdir -p artifacts/prod-known-hosts-hash-safe-repair
          evidence='artifacts/prod-known-hosts-hash-safe-repair/pre-state.env'
          {
            printf 'schema_version=1\n'
            printf 'request_id=%s\n' '${{ needs.validate-authority.outputs.request_id }}'
            printf 'authority_comment_id=%s\n' '${{ needs.validate-authority.outputs.authority_comment_id }}'
            printf 'repository_sha=%s\n' '${{ needs.validate-authority.outputs.main_sha }}'
            printf 'RUNNER=%s\n' "$RUNNER_NAME"
            printf 'MATCHING_ENTRY_COUNT_BEFORE=%s\n' "$(value MATCHING_ENTRY_COUNT "$classification")"
            printf 'PINNED_MATCH_COUNT_BEFORE=%s\n' "$(value PINNED_MATCH_COUNT "$classification")"
            printf 'NON_PINNED_MATCH_COUNT_BEFORE=%s\n' "$(value NON_PINNED_MATCH_COUNT "$classification")"
            printf 'PLAIN_MATCH_COUNT_BEFORE=%s\n' "$(value PLAIN_MATCH_COUNT "$classification")"
            printf 'HASHED_MATCH_COUNT_BEFORE=%s\n' "$(value HASHED_MATCH_COUNT "$classification")"
            printf 'KEY_TYPES_BEFORE=%s\n' "$(value KEY_TYPES "$classification")"
            printf 'ALIASED_MATCH_PRESENT_BEFORE=%s\n' "$(value ALIASED_MATCH_PRESENT "$classification")"
            printf 'MARKED_MATCH_PRESENT_BEFORE=%s\n' "$marked_match"
            printf 'REPAIR_STRATEGY=%s\n' "$(value REPAIR_STRATEGY "$classification")"
            printf 'PRESTATE_PERSISTED_BEFORE_REPAIR=PASS\n'
            printf 'PROD_SSH_SESSION=NONE\n'
            printf 'PROD_DB_READ=NONE\n'
            printf 'PROD_WRITE=NONE\n'
          } > "$evidence"
          chmod 600 "$evidence"

      - name: Upload metadata-only pre-state before repair
        uses: actions/upload-artifact@v4
        with:
          name: agency-prod-known-hosts-hash-safe-prestate-${{ needs.validate-authority.outputs.request_id }}-${{ github.run_id }}
          path: artifacts/prod-known-hosts-hash-safe-repair/pre-state.env
          if-no-files-found: error
          retention-days: 14

      - name: Hash-safe repair, exact verify and idempotency proof
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SERVER_HOST"
          unset AGENCY_842_SYNTHETIC_TEST AGENCY_842_TEST_FORCE_POST_VERIFY_FAILURE
          repair='scripts/production-ssh-trust/hash-safe-repair-known-hosts.sh'
          first="$(SERVER_HOST="$SERVER_HOST" bash "$repair" REPAIR)"
          known_hosts="$HOME/.ssh/known_hosts"
          first_hash="$(sha256sum "$known_hosts" | awk '{print $1}')"
          second="$(SERVER_HOST="$SERVER_HOST" bash "$repair" REPAIR)"
          second_hash="$(sha256sum "$known_hosts" | awk '{print $1}')"
          test "$first_hash" = "$second_hash"
          grep -Fxq 'REPAIR_EFFECTIVE_CHANGE=NO' <<< "$second"

          value() {
            local key="$1" data="$2"
            awk -F= -v key="$key" '$1 == key { print substr($0, index($0, "=") + 1); exit }' <<< "$data"
          }
          pre='artifacts/prod-known-hosts-hash-safe-repair/pre-state.env'
          evidence='artifacts/prod-known-hosts-hash-safe-repair/evidence.env'
          cat "$pre" > "$evidence"
          {
            printf 'MATCHING_ENTRY_COUNT_AFTER=%s\n' "$(value MATCHING_ENTRY_COUNT_AFTER "$first")"
            printf 'PINNED_MATCH_COUNT_AFTER=%s\n' "$(value PINNED_MATCH_COUNT_AFTER "$first")"
            printf 'NON_PINNED_MATCH_COUNT_AFTER=%s\n' "$(value NON_PINNED_MATCH_COUNT_AFTER "$first")"
            printf 'PLAIN_MATCH_COUNT_AFTER=%s\n' "$(value PLAIN_MATCH_COUNT_AFTER "$first")"
            printf 'HASHED_MATCH_COUNT_AFTER=%s\n' "$(value HASHED_MATCH_COUNT_AFTER "$first")"
            printf 'KEY_TYPES_AFTER=%s\n' "$(value KEY_TYPES_AFTER "$first")"
            printf 'ALIASED_MATCH_PRESENT_AFTER=%s\n' "$(value ALIASED_MATCH_PRESENT_AFTER "$first")"
            printf 'WORK_COPY_ONLY=%s\n' "$(value WORK_COPY_ONLY "$first")"
            printf 'WORK_COPY_TARGET_MATCH_COUNT_AFTER_REMOVAL=%s\n' "$(value WORK_COPY_TARGET_MATCH_COUNT_AFTER_REMOVAL "$first")"
            printf 'UNRELATED_STATE_PRESERVED=%s\n' "$(value UNRELATED_STATE_PRESERVED "$first")"
            printf 'KNOWN_HOSTS_MATCH=%s\n' "$(value KNOWN_HOSTS_MATCH "$first")"
            printf 'FINGERPRINT=%s\n' "$(value FINGERPRINT "$first")"
            printf 'ROLLBACK_USED=%s\n' "$(value ROLLBACK_USED "$first")"
            printf 'SECOND_REPAIR_EFFECTIVE_CHANGE=%s\n' "$(value REPAIR_EFFECTIVE_CHANGE "$second")"
            printf 'IDEMPOTENT_HASH=PASS\n'
          } >> "$evidence"
          chmod 600 "$evidence"

          grep -Fxq 'MATCHING_ENTRY_COUNT_AFTER=1' "$evidence"
          grep -Fxq 'PINNED_MATCH_COUNT_AFTER=1' "$evidence"
          grep -Fxq 'NON_PINNED_MATCH_COUNT_AFTER=0' "$evidence"
          grep -Fxq 'PLAIN_MATCH_COUNT_AFTER=1' "$evidence"
          grep -Fxq 'HASHED_MATCH_COUNT_AFTER=0' "$evidence"
          grep -Fxq 'KEY_TYPES_AFTER=ssh-ed25519' "$evidence"
          grep -Fxq 'ALIASED_MATCH_PRESENT_AFTER=NO' "$evidence"
          grep -Fxq 'MARKED_MATCH_PRESENT_BEFORE=NO' "$evidence"
          grep -Fxq 'WORK_COPY_ONLY=PASS' "$evidence"
          grep -Fxq 'WORK_COPY_TARGET_MATCH_COUNT_AFTER_REMOVAL=0' "$evidence"
          grep -Fxq 'UNRELATED_STATE_PRESERVED=PASS' "$evidence"
          grep -Fxq 'KNOWN_HOSTS_MATCH=PASS' "$evidence"
          grep -Fxq 'FINGERPRINT=SHA256:Pflpbgh2vc9dUYe4fpXPxdwzhPqyy8vmbAOcS+BRLDQ' "$evidence"
          grep -Fxq 'ROLLBACK_USED=NO' "$evidence"
          grep -Fxq 'SECOND_REPAIR_EFFECTIVE_CHANGE=NO' "$evidence"
          grep -Fxq 'IDEMPOTENT_HASH=PASS' "$evidence"
          grep -Fxq 'PROD_SSH_SESSION=NONE' "$evidence"
          grep -Fxq 'PROD_DB_READ=NONE' "$evidence"
          grep -Fxq 'PROD_WRITE=NONE' "$evidence"

      - name: Upload metadata-only terminal repair evidence
        uses: actions/upload-artifact@v4
        with:
          name: agency-prod-known-hosts-hash-safe-repair-${{ needs.validate-authority.outputs.request_id }}-${{ github.run_id }}
          path: artifacts/prod-known-hosts-hash-safe-repair/evidence.env
          if-no-files-found: error
          retention-days: 14
